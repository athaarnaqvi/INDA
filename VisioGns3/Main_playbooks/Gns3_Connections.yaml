---
- name: Create NLP-based connections in GNS3
  hosts: localhost
  gather_facts: no

  vars:
    gns3_server: "http://127.0.0.1:3080"
    project_name: "nlp_topology_20251209_193601"

  tasks:
    - name: Get all projects
      uri:
        url: "{{ gns3_server }}/v2/projects"
        method: GET
        return_content: yes
      register: gns3_projects

    - name: Set project ID
      set_fact:
        project_id: "{{ (gns3_projects.json | selectattr('name','equalto',project_name) | list)[0].project_id }}"
      when: gns3_projects.json | selectattr('name','equalto',project_name) | list | length > 0

    - name: Open project if needed
      uri:
        url: "{{ gns3_server }}/v2/projects/{{ project_id }}/open"
        method: POST
        status_code: [200, 201]

    - name: Get nodes
      uri:
        url: "{{ gns3_server }}/v2/projects/{{ project_id }}/nodes"
        method: GET
        return_content: yes
      register: gns3_nodes

    - name: Connect router1 to pc1
      vars:
        node_map: "{{ gns3_nodes.json | items2dict(key_name='name', value_name='node_id') }}"
      uri:
        url: "{{ gns3_server }}/v2/projects/{{ project_id }}/links"
        method: POST
        body_format: json
        headers:
          Content-Type: application/json
        status_code: [200, 201]
        body:
          nodes:
            - node_id: "{{ node_map['router1'] }}"
              adapter_number: 0
              port_number: 0
            - node_id: "{{ node_map['pc1'] }}"
              adapter_number: 0
              port_number: 0

    - name: Connect router1 to pc2
      vars:
        node_map: "{{ gns3_nodes.json | items2dict(key_name='name', value_name='node_id') }}"
      uri:
        url: "{{ gns3_server }}/v2/projects/{{ project_id }}/links"
        method: POST
        body_format: json
        headers:
          Content-Type: application/json
        status_code: [200, 201]
        body:
          nodes:
            - node_id: "{{ node_map['router1'] }}"
              adapter_number: 1
              port_number: 0
            - node_id: "{{ node_map['pc2'] }}"
              adapter_number: 0
              port_number: 0

    - name: Connect router1 to pc3
      vars:
        node_map: "{{ gns3_nodes.json | items2dict(key_name='name', value_name='node_id') }}"
      uri:
        url: "{{ gns3_server }}/v2/projects/{{ project_id }}/links"
        method: POST
        body_format: json
        headers:
          Content-Type: application/json
        status_code: [200, 201]
        body:
          nodes:
            - node_id: "{{ node_map['router1'] }}"
              adapter_number: 2
              port_number: 0
            - node_id: "{{ node_map['pc3'] }}"
              adapter_number: 0
              port_number: 0
